import SwiftUI
import AudioToolboxUI

@main
struct AudioToolboxApp: App {
    var body: some Scene {
        WindowGroup {
            PlaceholderView()
        }
        .defaultSize(width: 1100, height: 720)
    }
}
